server:
  port: 12011

mc:
  nacos:
    server-addr: 192.168.66.20:8848

spring:
  application:
    name: txlcn-demo-spring-service-a
  cloud:
    nacos:
      discovery:
        server-addr: ${mc.nacos.server-addr}