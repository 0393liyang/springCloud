server:
  port: 12002

mc:
  nacos:
    server-addr: 192.168.66.20:8848

spring:
  application:
    name: txlcn-demo-spring-service-b
  cloud:
    nacos:
      discovery:
        server-addr: ${mc.nacos.server-addr}